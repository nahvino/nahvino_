import 'package:flutter/material.dart';
class LastVisitProfile extends StatelessWidget {
  const LastVisitProfile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
